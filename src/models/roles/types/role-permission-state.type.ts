enum DefaultRoleStateType {
  'ENABLE' = 'ENABLE',
  'DISABLE' = 'DISABLE',
}

enum RoleStateType {
  'ENABLE' = 'ENABLE',
  'DISABLE' = 'DISABLE',
  'INHERIT' = 'INHERIT',
}

export { DefaultRoleStateType, RoleStateType };
